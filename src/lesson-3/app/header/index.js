import React from 'react';
import Styles from './styles.module.scss';

export const Header = ()=>{
    return (
        <section className = {Styles.header}>
            <h1>Header</h1>
        </section>
    )
}